"use strict";

/**
 * Convert an integer number into a byte string
 *
 * @todo implement the function without using Number.prototype.toString
 * @param {number} integer
 * @throw {IntegerToByteStringArgumentError} if there is not exactly one parameter
 * @throw {IntegerToByteStringTypeError} if the parameter is not a number
 * @throw {IntegerToByteStringNotIntegerError} if the parameter is not an integer number
 * @return {string}
 * @example
 * integerToByteString(15); // "1111"
 * integerToByteString(16); // "10000"
 */
export function integerToByteString() {}
